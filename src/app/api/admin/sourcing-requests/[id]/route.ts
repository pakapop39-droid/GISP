import{NextResponse,type NextRequest}from"next/server";import{apiError}from"@/lib/api/response";import{createInsForgeAdminClient}from"@/lib/insforge/admin";import{requireSourcingAdmin,sourcingRequestColumns}from"@/lib/sourcing/server";
export async function GET(_request:NextRequest,{params}:{params:Promise<{id:string}>}){try{await requireSourcingAdmin();const{id}=await params;const admin=createInsForgeAdminClient();const[requestRow,files,candidates,history]=await Promise.all([admin.database.from("product_sourcing_requests").select(`${sourcingRequestColumns},internal_note`).eq("id",id).maybeSingle(),admin.database.from("product_sourcing_files").select("file_id,sort_order").eq("request_id",id).order("sort_order").limit(8),admin.database.from("product_sourcing_candidates").select("*").eq("request_id",id).order("created_at").limit(20),admin.database.from("product_sourcing_history").select("id,action,from_status,to_status,message,visibility,created_at").eq("request_id",id).order("created_at").limit(200)]);for(const result of[requestRow,files,candidates,history])if(result.error)throw result.error;if(!requestRow.data)return NextResponse.json({message:"ไม่พบคำขอ"},{status:404});const fileIds=(files.data??[]).map(row=>row.file_id);const metadata=fileIds.length?await admin.database.from("file_metadata").select("id,original_name,mime_type,size_bytes").in("id",fileIds).limit(8):{data:[],error:null};if(metadata.error)throw metadata.error;return NextResponse.json({data:{request:requestRow.data,files:metadata.data??[],candidates:candidates.data??[],history:history.data??[]}})}catch(error){return apiError(error)}}

