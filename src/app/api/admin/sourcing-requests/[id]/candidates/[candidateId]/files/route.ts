import{randomUUID}from"node:crypto";import{NextResponse,type NextRequest}from"next/server";import{apiError}from"@/lib/api/response";import{validateSourcingImage}from"@/lib/files/image-upload";import{createInsForgeAdminClient}from"@/lib/insforge/admin";import{requireSourcingAdmin}from"@/lib/sourcing/server";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string;candidateId:string}>}){let key="",fileId="";try{const context=await requireSourcingAdmin();const{id,candidateId}=await params;const form=await request.formData();const file=form.get("file");const extension=file instanceof File?await validateSourcingImage(file):null;if(!(file instanceof File)||!extension)return NextResponse.json({message:"รองรับไฟล์ภาพ JPG, PNG และ WebP ที่ถูกต้อง ภาพละไม่เกิน 10 MB"},{status:400});const admin=createInsForgeAdminClient();const candidate=await admin.database.from("product_sourcing_candidates").select("id,status,request_id").eq("id",candidateId).eq("request_id",id).eq("status","DRAFT").maybeSingle();if(candidate.error)throw candidate.error;if(!candidate.data)return NextResponse.json({message:"ตัวเลือกสินค้านี้แก้ไขไม่ได้"},{status:409});const sourcingRequest=await admin.database.from("product_sourcing_requests").select("organization_id,member_profile_id,status").eq("id",id).in("status",["SUBMITTED","UNDER_REVIEW","NEED_INFO"]).maybeSingle();if(sourcingRequest.error)throw sourcingRequest.error;if(!sourcingRequest.data)return NextResponse.json({message:"คำขอนี้ไม่อยู่ในสถานะที่แนบภาพตัวเลือกได้"},{status:409});const count=await admin.database.from("product_sourcing_candidate_files").select("file_id",{count:"exact",head:true}).eq("candidate_id",candidateId);if((count.count??0)>=8)return NextResponse.json({message:"แนบภาพตัวเลือกได้สูงสุด 8 ภาพ"},{status:409});key=`sourcing-candidates/${id}/${candidateId}/${randomUUID()}.${extension}`;const uploaded=await admin.storage.from("gisp-member-private").upload(key,file);if(uploaded.error||!uploaded.data)throw uploaded.error??new Error("UPLOAD_FAILED");const stored=uploaded.data as unknown as{url?:string;key?:string};const metadata=await admin.database.from("file_metadata").insert([{organization_id:sourcingRequest.data.organization_id,member_profile_id:sourcingRequest.data.member_profile_id,bucket:"gisp-member-private",object_key:stored.key??key,url:stored.url??null,original_name:file.name,mime_type:file.type,size_bytes:file.size,visibility:"MEMBER_PRIVATE",entity_type:"PRODUCT_SOURCING_CANDIDATE",entity_id:candidateId,uploaded_by:context.userId}]).select("id").single();if(metadata.error||!metadata.data)throw metadata.error??new Error("FILE_METADATA_FAILED");fileId=metadata.data.id;const linked=await admin.database.from("product_sourcing_candidate_files").insert([{candidate_id:candidateId,file_id:fileId,sort_order:count.count??0}]);if(linked.error)throw linked.error;return NextResponse.json({data:{id:fileId},message:"แนบภาพตัวเลือกแล้ว"},{status:201})}catch(error){const admin=createInsForgeAdminClient();if(fileId)await admin.database.from("file_metadata").delete().eq("id",fileId);if(key)await admin.storage.from("gisp-member-private").remove(key);return apiError(error)}}
