import{NextResponse,type NextRequest}from "next/server";import{apiError}from "@/lib/api/response";import{requireAppAccess}from "@/lib/auth/session";import{createInsForgeServerClient}from "@/lib/insforge/server";
export async function POST(_request:NextRequest,{params}:{params:Promise<{itemId:string}>}){try{await requireAppAccess({active:true});const{itemId}=await params;const db=await createInsForgeServerClient();const{data,error}=await db.database.rpc("set_project_item_ready",{project_item_id_input:itemId});if(error)throw error;return NextResponse.json({data,message:"รายการพร้อมสั่งซื้อแล้ว"})}catch(error){return apiError(error)}}