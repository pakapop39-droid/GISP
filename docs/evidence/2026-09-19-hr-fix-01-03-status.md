# GISP Hosted Rehearsal HR-FIX-01–03 — Status

Date: 2026-09-19

Approval: `docs/GISP-APR-HR-FIX-01-03-IMPLEMENTATION-20260919-v1.0.md`

Production allowed: No

## Frozen local candidate

- Branch integrated into `development` by fast-forward.
- Commit: `11db3010245b3094120903fa4c2623e5e6b2473c`.
- Tree: `eb30e105663609bd01815f33884f23be13ea513b`.
- HR-FIX-02/03 Local QA verdict: `PASS WITH CONDITIONS`; Hosted evidence is not yet passed.

## Implemented locally

- Forward Resume restores both private Payment function blocks byte-for-byte from the frozen prerequisite.
- A deterministic read-only 54-function snapshot captures exact definitions, owner, language, volatility, security mode, sorted configuration and expanded/sorted ACL with per-row and combined hashes.
- A non-secret release-attestation endpoint binds exact candidate commit/tree, Child project ID, exact InsForge backend host/app key, release stage/slices and a boolean head-only server-admin probe.
- Hosted evidence capture sanitizes secret fields and connection strings and records raw/sanitized SHA-256 values without storing raw secrets in the retained evidence package.
- The release gate permits only the exact attestation path; lookalike paths remain blocked.

## Test evidence

- Focused tests: `38/38` passed.
- Full regression: `98` files / `518` tests passed.
- TypeScript check: passed.
- Production build: passed with `128` routes.
- Targeted ESLint: passed.
- Independent adversarial QA: `5/5` passed and temporary QA artifacts were removed.

## Open platform blocker — HR-FIX-01

- Child `e902393a-ffe7-433d-96d8-a37256948959` still contains active schedule metadata ID `aefecf8b-5b02-4649-a9b2-47aa448af0e8` pointing to the Production notification URL.
- The normal update/delete API fails because referenced Cron Job `4` is absent.
- The managed `schedules.jobs` table is owned by `postgres`; `project_admin` has no `DELETE` or `UPDATE` privilege. No privilege escalation or managed-schema bypass was attempted.
- InsForge feedback requesting Child-only metadata repair: `6d447cda-4a7a-4e23-99d8-af0c2a845cfb`.
- A fresh read-only check after the owner's instruction to continue still showed the Child schedule active with the Production URL, while the Parent schedule remained active and continued executing normally. The authorized standard Child-only delete was retried once and failed again with `could not find valid entry for job 4`; no metadata row changed.
- Hosted Deploy, Apply, new Backup, Restore and Runtime Retest remain stopped until the Child schedule is observably absent/inactive and the Parent Production schedule is verified unchanged.

## Dependency risk before Production

The unchanged dependency tree contains current Next.js and Sharp security advisories. Existing input and image configuration reduce the identified AVIF/HEIF paths, but the dependency risk must be upgraded under a separately approved scope or explicitly accepted with hosted-runtime mitigation evidence before Production Release Authorization.

## Remaining work

1. InsForge repairs/removes only the ghost Child schedule metadata and the executor verifies the Parent schedule is unchanged.
2. Run the single authorized Hosted Rehearsal against this exact candidate, retain durable evidence, restore once, and obtain independent QA PASS.

This status is not Production Release Authorization.
