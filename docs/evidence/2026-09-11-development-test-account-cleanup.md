# Development test account cleanup — 11 กันยายน 2569

## สถานะ

**สำรองครบแล้ว แต่ยังไม่ได้ลบข้อมูล เนื่องจากสิทธิ์ฐานข้อมูลไม่อนุญาต**

ผู้ใช้ยืนยันให้ลบเฉพาะบัญชีทดสอบอัตโนมัติ 15 บัญชี พร้อมข้อมูลที่เกี่ยวข้อง ในโครงการ `gisp-mvp-development` (`kit6y4pj`) โดยเก็บสมาชิกอีก 4 บัญชี รวม UAT กลาง และไม่เปลี่ยนสถานะ Slice เดิม

## ขอบเขตที่ตรวจพบ

- Dashboard A/B จำนวน 4 บัญชี และ Slice test member จำนวน 11 บัญชี
- สมาชิก/คำขอสมัคร/องค์กรทดสอบ อย่างละ 15 รายการ
- โครงการ 6 รายการ, คำขอผลิต 8 รายการ, ใบเสนอราคา 10 รายการ
- ข้อมูลไฟล์ 12 รายการ: พบไฟล์จริงและสำรองได้ 11 ไฟล์; อีก 1 ไฟล์หายไปก่อนเริ่มงาน
- เมื่อรวมตารางลูก ประวัติทดสอบ เซสชัน สิทธิ์ และการมอบหมายงาน มีข้อมูลใน public ที่อยู่ในแผนลบ 447 แถว
- ไม่มีคำสั่งซื้อ เคลม สินค้าจริง หรือข้อมูลสมาชิกที่ต้องเก็บอยู่ในแผนลบ

## ชุดสำรองภายในเครื่อง

ตำแหน่ง `C:/codex/GISP/output/backups/account-cleanup-2026-09-11/`

- `public-complete-before.json`: สำรองครบ 116 ตาราง พร้อมโครงสร้างและข้อมูล ตรวจแล้วไม่ถูกตัดจำนวนแถว
- SHA-256: `32e41537e68b476796635e7e861384a4951579c06c10cb3f05726dc989d1cada`
- `auth-and-storage-before.json`: ข้อมูล Auth สำหรับบัญชีทดสอบและรายการ Storage สำหรับการตรวจสอบ
- `files/` และ `files-manifest.json`: สำเนาไฟล์จริง 11 ไฟล์ ตรวจขนาดและ SHA-256 ครบ
- ไฟล์เหล่านี้อยู่ใน `output/` ซึ่ง Git ไม่ติดตาม และมีข้อมูลส่วนบุคคล/ข้อมูลยืนยันตัวตน ห้ามเผยแพร่
- การสร้าง backup บน InsForge ไม่สำเร็จ เพราะใช้โควตา manual backup เต็ม 5/5; ไม่ได้ลบ backup เก่า

## ข้อจำกัดที่พบ

1. ทดลอง SQL แบบ rollback ด้วยสิทธิ์ปกติแล้วติด `must be owner of table security_events` จึงยังไม่ผ่านการทดลองลบ
2. ช่องทาง SQL ระดับผู้ดูแลที่ CLI รองรับตอบ `Unrestricted SQL execution is disabled on this project.`
3. API ลบผู้ใช้ที่เอกสารรองรับ (`DELETE /api/auth/users` โดยระบุ userIds 15 รายการ) ตอบ HTTP 400 `DATABASE_CONSTRAINT_VIOLATION` ที่ `user_roles_assigned_by_fkey`

จำเป็นต้องมีช่องทางบำรุงรักษาฐานข้อมูลที่ได้รับอนุญาตในระดับเจ้าของตารางก่อนดำเนินการต่อ การยืนยันขอบเขตจากผู้ใช้มีแล้ว ไม่ต้องขอยืนยันรายการเดิมซ้ำ

แผน SQL ใน `output/account-cleanup-2026-09-11/` เป็นร่างที่ยัง **ไม่ผ่าน dry run** และยังไม่ได้ใช้ลบจริง มีเงื่อนไขล็อกตาราง ตรวจ ID/จำนวนแถว และคืน trigger ภายใน transaction เดียว ห้ามนำไปใช้โดยไม่ตรวจสิทธิ์และทดลอง rollback ให้ผ่านก่อน

## ผลตรวจหลังการทดลอง

- บัญชีเป้าหมายยังอยู่ครบ 15 บัญชี และสมาชิกที่เก็บไว้ครบ 4 บัญชี
- เปรียบเทียบข้อมูลก่อน/หลังครบ 116 ตาราง: เหมือนเดิมทุกแถว
- รายการ Storage เหมือนเดิมทั้งหมด และไฟล์สำรองผ่านการตรวจ hash ทุกไฟล์
- Trigger ที่ตรวจยังเปิดใช้งานทั้งหมด ไม่มีการปิดการป้องกันค้างไว้
- ไม่มีการลบข้อมูลสำเร็จหรือเหลือข้อมูลที่ถูกลบค้างบางส่วน
- รายงานเครื่องอ่านได้: `output/account-cleanup-2026-09-11/verification-result.json`

## งานที่เหลือ

เหลือ 2 ขั้นตอนสำหรับงานเคลียร์นี้:

1. ใช้ช่องทางที่มีสิทธิ์เจ้าของฐานข้อมูล ทดลองแผนให้ผ่าน แล้วลบบัญชี/ข้อมูล/ไฟล์ตามขอบเขตที่ยืนยัน
2. ตรวจว่าบัญชีเป้าหมายเหลือ 0 และข้อมูลที่ต้องเก็บยังอยู่ครบ

สถานะ Slice เดิมไม่เปลี่ยน
