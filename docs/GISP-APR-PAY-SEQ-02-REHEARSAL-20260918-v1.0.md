# GISP PAY-SEQ-02 — Isolated Rehearsal Authorization Record v1.0

- `approval_level`: Scope Approved + Implementation Authorized for an isolated rehearsal only; **not Production Release Authorized**.
- `approved_by`: ภคภพ ช.เจริญยิ่ง, owner, in this conversation.
- `approval_text_or_reference`: “ข้าพเจ้า ภคภพ ช.เจริญยิ่ง อนุมัติ PAY-SEQ-02 ให้ Codex สร้างพื้นที่ซ้อมที่แยกจาก Production B ตรวจ Project ID, Migration, สิทธิ์และ Hash ก่อน Apply เฉพาะ SQL 12 ไฟล์ในแพ็กเกจ PAY-RPC-GATE-001 ตามลำดับ ใช้บัญชีและข้อมูลจำลอง `REH-PAY-RPC-001` ทดสอบหลักฐาน Payment, การอนุมัติ/ปฏิเสธ, สิทธิ์ข้ามองค์กร, Audit และยอด Deposit/Balance/Freight พร้อมให้ QA ตรวจอิสระ ไม่อนุญาตให้แก้หรือ Deploy Production เปิด Release C/D หรือใช้เงินจริง หากตรวจพบความไม่ตรงกันให้หยุดก่อน Apply และรายงาน”
- `scope_document_version`: `docs/GISP-PLAN-PAY-RPC-GATE-001-v0.1.md`, `release-candidates/pay-rpc-gate-001/README.md` provisional package; PAY-SEQ-02.
- `environment`: a newly isolated Production-B-derived rehearsal environment only; not the Production parent or existing Development project.
- `data_migration_authorized`: No migration of existing business data. The 12 reviewed SQL migration files may be applied **only** to the verified isolated rehearsal environment after Project ID, lineage, ACL/signature/dependency and hash checks. Synthetic accounts, rows and files marked `REH-PAY-RPC-001` may be created there for the approved tests. No modification or deletion of existing data.
- `production_allowed`: No Production database/configuration/storage writes, deployment, transaction, Release C/D activation or real money.
- `approved_at`: recorded on receipt at `2026-09-18T22:26:26+07:00` (Asia/Bangkok); this is the record time, not a claim about the message-send time.

## Pre-apply stop condition observed

On read-only preflight, the Production parent `gisp-mvp-production` (project ID `865860c2-49fa-4e53-908f-9396b2f75233`) had two active ready schema-only branches: `release-b-rehearsal-20260908` and `production-completion-20260906`. InsForge documents a maximum of two active branches per parent. Creating a new branch is therefore not presently available without freeing capacity or an externally approved alternative. Resetting/deleting either existing branch would alter its existing state and is **not** authorized by this record. No SQL migration has been applied under PAY-SEQ-02.

The local package still matched all 12 SQL and 11 app/test SHA-256 values (23/23) at preflight. The Production parent migration head was `20260908170000_release-b-member-pilot` with 26 applied migrations and 0 Customer Orders / 0 Payment Transfers / 4 auth users in the targeted count query. Existing branch `release-b-rehearsal-20260908` had the same displayed migration head but 11 auth users, so it is not an unused empty branch. Existing branch `production-completion-20260906` had a different migration head (`20260908150000_release-b-shared-catalog-sourcing`), 28 applied migrations, 10 Customer Orders, 11 Payment Transfers and 31 auth users. Neither existing branch is a fresh current-Production-B-derived rehearsal target. Their earlier snapshots and all existing state must be preserved until the owner decides otherwise. The **separate temporary CLI context** used for read-only inspection was switched back to the Production parent; the shared repository context remained linked to Development. No backend state was changed by inspection.

Read-only live function/ACL inspection found `public.verify_payment_transfer(uuid,boolean,text)` remains executable by `authenticated` in Production B. Its internal permission guard requires `payments.verify`, but it does not scope that permission to the target Payment organization; its approval path does not bind to a stored evidence file, and NULL approval input is not explicitly rejected. This is a **financial/security blocker for opening Release C**, not a claim that all authenticated users can approve. At the inspected time Production had no Payment Transfers. The candidate C safety revoke/private executor is intended to close this path, but must pass isolated rehearsal before any Production authorization. The live no-duplicate-shipment-milestone trigger is installed/enabled; 39 of 43 package GRANT signatures already exist, and the 4 absent signatures are package-created prerequisites.

**Status:** PAY-SEQ-02 preflight QA = FAIL / STOP BEFORE APPLY (environment capacity blocker, not a local source-code failure). An owner decision is required on exact rehearsal capacity/target. This record does not authorize resetting or deleting an existing branch, purchasing more capacity, substituting a stale branch, or applying to Production/Development.
